import { SafeAreaView, StyleSheet, Text, View } from "react-native";
import React from "react";

export const SocialShareScreen = () => {
  return (
    <SafeAreaView>
      <Text>SocialShareScreen</Text>
    </SafeAreaView>
  );
};

const styles = StyleSheet.create({});
